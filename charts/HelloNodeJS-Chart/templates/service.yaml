apiVersion: v1
kind: Service
metadata:
  name: {{ template "nodeapp-chart.fullname" . }}
  labels:
    app: {{ template "nodeapp-chart.name" . }}
    chart: {{ template "nodeapp-chart.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "nodeapp-chart.name" . }}
    release: {{ .Release.Name }}
